# MarkerList

`MarkerList` is a now-deprecated package for working with the output of
`Seurat::FindMarkers`. The functions might be refactored and used for the
development of another marker-themed package in the future.

## Installation

To install `MarkerList`, run the following R code:

```
pak::pak('andrei-stoica26/MarkerList')
```
